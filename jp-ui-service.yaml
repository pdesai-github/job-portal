apiVersion: v1
kind: Service
metadata:
  name: job-portal-ui-service
spec:
  selector:
    app: job-portal-ui
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30000
  type: NodePort
  